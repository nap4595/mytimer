/**
 * Main application entry point.
 * Initializes the router and starts the application.
 */
import { initRouter } from './shared/core/router.js';

// Initialize the router to handle page navigation
initRouter();
